var { createCanvas } = require('canvas');

module.exports = ( curves, width, height ) => {
    var canvas = createCanvas( width, height );
    var ctx = canvas.getContext( '2d' );
    // ctx.fillStyle = 'black';
    // ctx.fillRect( 0, 0, canvas.width, canvas.height );
    
}